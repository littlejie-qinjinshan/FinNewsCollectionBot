# 财经新闻采集机器人 - 使用说明

## 📋 功能说明

该程序可以自动从多个财经媒体抓取新闻，生成Word文档，并定时发送到指定邮箱。

## 🚀 快速开始

### 1. 测试运行
双击运行 `test_quick.py` 或在命令行执行：
```bash
python test_quick.py
```
这会快速测试邮件发送功能。

### 2. 单次执行
双击运行 `立即执行任务.bat` 或在命令行执行：
```bash
python financebot.py --once
```
这会立即执行一次完整的新闻采集和推送任务。

### 3. 定时任务模式
双击运行 `启动定时任务.bat` 或在命令行执行：
```bash
python financebot.py
```
- 程序会立即执行一次任务
- 之后每天在配置的时间自动执行
- 保持窗口打开，程序将持续运行
- 按 Ctrl+C 可退出程序

## ⚙️ 配置说明

编辑 `.env` 文件进行配置：

```env
# OpenAI/DeepSeek 配置（可选，用于AI摘要）
OPENAI_API_KEY=your_api_key_here

# 邮件配置
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_USER=your_email@163.com
SMTP_PASSWORD=your_authorization_code
RECIPIENT_EMAIL=recipient1@example.com,recipient2@example.com

# 定时任务时间（24小时制）
SCHEDULE_TIME=09:00

# HTTP服务（可选）
ENABLE_HTTP=true
HTTP_PORT=8080
```

## 📁 文件说明

- `financebot.py` - 主程序文件
- `test_quick.py` - 快速测试脚本
- `requirements.txt` - Python依赖包
- `.env` - 配置文件
- `output/` - 生成的Word文档保存目录
- `启动定时任务.bat` - 定时任务启动脚本
- `立即执行任务.bat` - 单次任务执行脚本

## 📧 获取163邮箱授权码

1. 登录163邮箱
2. 进入「设置」→「POP3/SMTP/IMAP」
3. 开启「IMAP/SMTP服务」
4. 按照提示获取授权码

## 🌐 可选：使用HTTP界面

如果启用了HTTP服务（`ENABLE_HTTP=true`），可以：

1. 打开浏览器访问 `http://localhost:8080`
2. 点击「立即执行一次任务」按钮手动触发推送

## 📝 RSS源说明

程序内置了以下媒体的RSS源：
- 财经媒体：华尔街见闻、东方财富、香港经济日报、中新网财经、人民日报财经
- 科技媒体：36氪、虎嗅网、钛媒体、爱范儿、IT之家
- 综合新闻：人民日报时政、人民日报国际
- 国际财经：华尔街日报、MarketWatch、BBC商业、CNBC等
- A股市场：个股频道、综合频道、个股异动等
- 港股市场：港交所参与者通告、港交所研究资料
- 美股市场：SeekingAlpha、纳斯达克、SEC文件

## 🔧 故障排除

### 邮件发送失败
- 检查SMTP配置是否正确
- 确认授权码（不是邮箱密码）是否正确
- 检查网络连接

### RSS抓取失败
- 检查网络连接
- 部分RSS源可能需要代理

### 程序无法启动
- 确认已安装Python 3.7+
- 确认已安装依赖包：`pip install -r requirements.txt`
